name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'npm tag (e.g. latest, beta, next)'
        required: true
        default: 'latest'

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Setup Node for npm publish
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Publish to npm
        run: |
          TAG="${{ github.event.inputs.tag || 'latest' }}"
          if [[ "${{ github.ref_name }}" == *"beta"* ]]; then TAG="beta"; fi
          if [[ "${{ github.ref_name }}" == *"alpha"* ]]; then TAG="alpha"; fi
          npm publish --tag $TAG --provenance --access public

      - name: Publish to GitHub Packages
        run: |
          pnpm config set //npm.pkg.github.com/:_authToken ${GITHUB_TOKEN}
          if [[ $(node -p "require('./package.json').name") != "@"* ]]; then
            node -e "const pkg = require('./package.json'); pkg.name = '@' + process.env.OWNER.toLowerCase() + '/' + pkg.name; require('fs').writeFileSync('./package.json', JSON.stringify(pkg, null, 2))"
          fi
          pnpm publish --registry https://npm.pkg.github.com/ --no-git-checks
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OWNER: ${{ github.repository_owner }}